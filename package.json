{
  "name": "dumpy-ecom-proj-root",
  "private": true,
  "scripts": {
    "install-admin": "npm --prefix admin install",
    "build": "npm --prefix admin install && npm --prefix admin run build",
    "dev": "npm --prefix admin run dev",
    "start": "npm --prefix admin run preview"
  }
}